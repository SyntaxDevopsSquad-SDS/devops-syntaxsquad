services:
  whoknows:
    build:
      context: .
      dockerfile: implementations/go/Dockerfile
    env_file:
      - .env
    environment:
      DB_PATH: /data/whoknows.db
    ports:
      - "8080:8080"
    volumes:
      - whoknows-data:/data

volumes:
  whoknows-data:
